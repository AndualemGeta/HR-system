generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum EmploymentType {
  FULL_TIME
  PART_TIME
  COMMISSION_BASED
  CONTRACT
  INTERN
  TEMPORARY
  OTHER
}

enum EmploymentStatus {
  DRAFT
  ONBOARDING
  ACTIVE
  ON_PROBATION
  SUSPENDED
  ON_LEAVE
  TRANSFERRED
  RESIGNED
  TERMINATED
  EXITED
}

enum EmployeeCategory {
  HEAD_OFFICE
  SHOP_FIELD
}

enum EmployeeRole {
  CEO
  CEO_COORDINATOR
  SALES_HEAD
  ASM
  SHOP_MANAGER
  SHOP_ACCOUNTANT
  DSA
  DSP
  BA_COORDINATOR
  BUSINESS_DEVELOPMENT_MANAGER
  CLEANING_STAFF
  SECURITY_STAFF
  EBU_SUPERVISOR
  EBU_FTTH_SUPERVISOR
  EBU_TECHNICAL_SALES_LEAD
  EBU_FTTH_SALES
  DISTRIBUTION_MANAGER
  DISTRIBUTION_OFFICER
  FINANCE_DIRECTOR
  TREASURY_MANAGER
  ACCOUNTANT
  FINANCIAL_CONTROL_REPORTING_MANAGER
  HR_MANAGER
  HR_OFFICER
  TECHNOLOGY_MANAGER
  EMPLOYEE
  OTHER
}

enum EmployeeLevel {
  JUNIOR
  MID
  SENIOR
  LEAD
  MANAGER
  DIRECTOR
  EXECUTIVE
  TO_BE_DEFINED
}

enum LocationType {
  DIVISION
  REGION
  AREA
  SHOP
  CLUSTER
}

enum DocumentType {
  ID
  CONTRACT
  CV
  CERTIFICATE
  EMERGENCY_CONTACT
  BANK_OR_PAYMENT_INFORMATION
  TAX_OR_PAYROLL_INFORMATION
  COMMISSION_AGREEMENT
  ASSIGNMENT_LETTER
  RESPONSIBILITY_DOCUMENT
  CONFIDENTIALITY_DOCUMENT
  WARNING_LETTER
  TERMINATION_LETTER
  RESIGNATION_LETTER
  CLEARANCE
  EVALUATION_DOCUMENT
  SALARY_DOCUMENT
  OTHER
}

enum DocumentVisibility {
  PUBLIC_TO_HR
  MANAGER_VISIBLE
  EMPLOYEE_VISIBLE
  SENSITIVE_HR_ONLY
  SALARY_RESTRICTED
}

enum LeaveType {
  ANNUAL
  SICK
  MATERNITY
  PATERNITY
  UNPAID
  EMERGENCY
  COMPASSIONATE
  OTHER
}

enum EvaluationType {
  PROBATION
  MONTHLY
  QUARTERLY
  ANNUAL
  SHOP_MANAGER
  DISCIPLINARY_FOLLOW_UP
  PROMOTION_READINESS
  COMMISSION_REVIEW
  SALARY_REVIEW
  OTHER
}

enum EvaluationStatus {
  DRAFT
  SUBMITTED
  REVIEWED
  APPROVED
  REJECTED
}

enum ApprovalStatus {
  DRAFT
  SUBMITTED
  PENDING
  APPROVED
  REJECTED
  CANCELLED
}

enum DisciplinaryStatus {
  DRAFT
  SUBMITTED
  UNDER_REVIEW
  APPROVED
  REJECTED
  ESCALATED
  CLOSED
}

enum TerminationStatus {
  DRAFT
  SUBMITTED
  UNDER_REVIEW
  APPROVED
  REJECTED
  EXIT_IN_PROGRESS
  EXIT_COMPLETED
  CANCELLED
}

enum SalaryReviewStatus {
  DRAFT
  SUBMITTED
  HR_REVIEW
  FINANCE_REVIEW
  APPROVED
  REJECTED
  CANCELLED
  COMPLETED
}

enum PayrollBatchStatus {
  DRAFT
  VALIDATED
  APPROVED
  CANCELLED
}

enum PayrollCalculationStatus {
  PENDING
  RUNNING
  COMPLETED
  FAILED
  SUPERSEDED
}

enum PayrollReadiness {
  READY
  WARNING
  BLOCKED
}

enum PayrollLockStatus {
  UNLOCKED
  LOCKED
  UNLOCK_REQUESTED
}

enum CalculationLineType {
  BASIC_SALARY
  EARNING
  ALLOWANCE
  COMMISSION
  BONUS
  OVERTIME
  ADJUSTMENT
  EMPLOYEE_PENSION
  EMPLOYER_PENSION
  PAYE_TAX
  DEDUCTION
  EMPLOYER_CONTRIBUTION
}

enum CalculationSourceType {
  EMPLOYEE_SALARY
  PAYROLL_INPUT
  PAY_RULE
  PAYROLL_ADJUSTMENT
  ATTENDANCE
  STATUTORY_RULE
  SYSTEM
}

enum PensionBaseType {
  BASIC_SALARY
  PENSIONABLE_EARNINGS
  CUSTOM_COMPONENTS
}

enum CommissionCalculationType {
  FIXED_AMOUNT
  PERCENT_OF_SALES
  TIERED_PERCENT
  TARGET_BASED
  MANUAL
}

enum CommissionCalculationStatus {
  DRAFT
  CALCULATED
  UNDER_REVIEW
  APPROVED
  REJECTED
}

enum AllowanceType {
  KPI_ALLOWANCE
  TRANSPORT_ALLOWANCE
  HOUSING_ALLOWANCE
  POSITION_ALLOWANCE
  MEAL_ALLOWANCE
  COMMUNICATION_ALLOWANCE
  OTHER_ALLOWANCE
}

enum DeductionType {
  LOAN_DEDUCTION
  ADVANCE_DEDUCTION
  ABSENCE_DEDUCTION
  DAMAGE_DEDUCTION
  PENALTY_DEDUCTION
  OTHER_DEDUCTION
}

enum NotificationType {
  APPROVAL_REQUIRED
  APPROVAL_COMPLETED
  REQUEST_REJECTED
  CHANGES_REQUESTED
  DOCUMENT_UPLOADED
  REMINDER
  SYSTEM
}

enum PayComponentType {
  BASIC_SALARY
  ALLOWANCE
  KPI
  TRANSPORT
  OVERTIME
  COMMISSION
  BONUS
  ADJUSTMENT
  DEDUCTION
  STATUTORY
  OTHER
}

enum TaxTreatment {
  TAXABLE
  NON_TAXABLE
  PARTIALLY_TAXABLE
  STATUTORY
  UNKNOWN
}

enum PayRuleStatus {
  DRAFT
  ACTIVE
  INACTIVE
  EXPIRED
}

enum PayRuleType {
  FIXED_AMOUNT
  PERCENTAGE
  THRESHOLD
  TIERED
  PER_UNIT
  MANUAL_INPUT
  FORMULA
  CAP_ONLY
}

enum ThresholdMetric {
  SALES_ACHIEVEMENT_PERCENT
  DAILY_ACTIVE_MERCHANT
  GA_COUNT
  QGA_COUNT
  RECHARGE_COUNT
  ATTENDANCE_DAYS
  OVERTIME_HOURS
  MANUAL_VALUE
}

enum PayrollPeriodStatus {
  DRAFT
  OPEN_FOR_INPUT
  INPUT_COLLECTION_CLOSED
  REVIEW_IN_PROGRESS
  READY_FOR_CALCULATION
  READY_FOR_REVIEW
  CANCELLED
}

enum PayrollInputTypeCategory {
  ALLOWANCE
  DEDUCTION
  COMMISSION
  KPI
  TRANSPORT
  OVERTIME
  BONUS
  ADJUSTMENT
  OTHER
}

enum PayrollInputTypeValueType {
  AMOUNT
  NUMBER
  PERCENTAGE
  BOOLEAN
  TEXT
}

enum PayrollInputSource {
  MANUAL
  IMPORT
  SYSTEM
}

enum PayrollInputStatus {
  DRAFT
  SUBMITTED
  RETURNED
  ACCEPTED
  REJECTED
}

enum CorridorType {
  CORRIDOR
  NON_CORRIDOR
  UNKNOWN
}

enum ShopCriteria {
  GOLD
  SILVER
  BRONZE
  AT_RISK
  UNASSIGNED
}

enum IncentivePeriodStatus {
  DRAFT
  OPEN
  CALCULATED
  CANCELLED
}

enum AuditAction {
  LOGIN
  LOGOUT
  FAILED_LOGIN
  EMPLOYEE_CREATE
  EMPLOYEE_UPDATE
  EMPLOYEE_STATUS_CHANGE
  SALARY_CHANGE
  ASSIGNMENT_CHANGE
  MANAGER_CHANGE
  ACCOUNTING_MANAGER_CHANGE
  DOCUMENT_UPLOAD
  DOCUMENT_VIEW
  DOCUMENT_DEACTIVATE
  LEAVE_CREATE
  LEAVE_APPROVAL
  LEAVE_REJECTION
  EVALUATION_CREATE
  EVALUATION_SUBMIT
  EVALUATION_APPROVAL
  EVALUATION_REJECTION
  DISCIPLINARY_CREATE
  DISCIPLINARY_APPROVAL
  TERMINATION_CREATE
  TERMINATION_APPROVAL
  TERMINATION_COMPLETE_EXIT
  TRANSFER_CREATE
  TRANSFER_APPROVAL
  TRANSFER_COMPLETE
  PROMOTION_CREATE
  PROMOTION_APPROVAL
  PROMOTION_COMPLETE
  SALARY_REVIEW_CREATE
  SALARY_REVIEW_APPROVAL
  SALARY_REVIEW_COMPLETE
  COMMISSION_CREATE
  COMMISSION_APPROVAL
  PAYROLL_BATCH_CREATE
  PAYROLL_BATCH_APPROVE
  PAYROLL_BATCH_EXPORT
  PAYROLL_PERIOD_LOCK
  PAYROLL_ADJUSTMENT
  IMPORT_UPLOAD
  IMPORT_APPROVE
  EXPORT_CREATE
  USER_CREATE
  USER_PERMISSION_CHANGE
  SYSTEM_SETTING_UPDATE
  PROFILE_CHANGE_REQUEST
  PROFILE_CHANGE_APPROVAL
  DATA_QUALITY_RESOLVE
  DOCUMENT_DOWNLOAD
  DOCUMENT_UPDATE
  DOCUMENT_RULE_CREATE
  DOCUMENT_RULE_UPDATE
  DOCUMENT_RULE_DEACTIVATE
  ONBOARDING_UPDATE
  ONBOARDING_COMPLETE
  ONBOARDING_OVERRIDE
  EMPLOYEE_IMPORT_PREVIEW
  EMPLOYEE_IMPORT_CONFIRM
  EMPLOYEE_IMPORT_CREATE
  EMPLOYEE_IMPORT_UPDATE
  EMPLOYEE_IMPORT_SKIP
  PAYROLL_READINESS_VIEW
  PAYROLL_READINESS_EXPORT
  PAYROLL_PROFILE_UPDATE
  PAY_COMPONENT_CREATE
  PAY_COMPONENT_UPDATE
  PAY_COMPONENT_DEACTIVATE
  PAY_RULE_CREATE
  PAY_RULE_UPDATE
  PAY_RULE_ACTIVATE
  PAY_RULE_DEACTIVATE
  PAY_RULE_PREVIEW
  DATA_QUALITY_SCAN
  DATA_QUALITY_IGNORE
  CHANGE_REQUEST_CREATE
  CHANGE_REQUEST_APPROVE
  CHANGE_REQUEST_APPLY
  CHANGE_REQUEST_REJECT
  CHANGE_REQUEST_CANCEL
  SALARY_RULE_APPROVAL_REQUEST
  SALARY_RULE_APPROVAL_APPROVE
  SALARY_RULE_APPROVAL_REJECT
  PHASE_CONTROL_UPDATE
  PAYROLL_PERIOD_CREATE
  PAYROLL_PERIOD_UPDATE
  PAYROLL_PERIOD_OPEN
  PAYROLL_PERIOD_CLOSE
  PAYROLL_PERIOD_CANCEL
  PAYROLL_EMPLOYEE_ADD
  PAYROLL_EMPLOYEE_REMOVE
  PAYROLL_INPUT_TYPE_CREATE
  PAYROLL_INPUT_TYPE_UPDATE
  PAYROLL_INPUT_TYPE_DEACTIVATE
  PAYROLL_INPUT_CREATE
  PAYROLL_INPUT_UPDATE
  PAYROLL_INPUT_SUBMIT
  PAYROLL_INPUT_ACCEPT
  PAYROLL_INPUT_REJECT
  PAYROLL_INPUT_RETURN
  PAYROLL_INPUT_IMPORT_PREVIEW
  PAYROLL_INPUT_IMPORT_CONFIRM
  PAYROLL_INPUT_REQUIREMENT_CREATE
  PAYROLL_INPUT_REQUIREMENT_UPDATE
  PAYROLL_INPUT_REQUIREMENT_DEACTIVATE
  PAYROLL_MISSING_INPUT_CHECK
  PAYROLL_INPUT_LOCK
  PAYROLL_INPUT_UNLOCK
  PAYROLL_INPUT_LOCK_ACCEPTED
  PAYROLL_INPUT_WAIVER_CREATE
  PAYROLL_INPUT_WAIVER_DEACTIVATE
  PAYROLL_PERIOD_REVIEW_START
  PAYROLL_PERIOD_READY_FOR_CALCULATION
  PAYROLL_PREPARATION_SUMMARY_EXPORT
  SHOP_CREATE
  SHOP_UPDATE
  SHOP_DEACTIVATE
  SHOP_REACTIVATE
  SHOP_MANAGER_ASSIGN
  SHOP_CRITERIA_UPDATE
  SHOP_PROFILE_UPDATE
  SHOP_MANAGER_INCENTIVE_PERIOD_CREATE
  SHOP_MANAGER_INCENTIVE_PERIOD_UPDATE
  SHOP_MANAGER_INCENTIVE_PERIOD_OPEN
  SHOP_MANAGER_INCENTIVE_INPUT_CREATE
  SHOP_MANAGER_INCENTIVE_INPUT_UPDATE
  SHOP_MANAGER_INCENTIVE_INPUT_DELETE
  SHOP_MANAGER_INCENTIVE_CALCULATE
  SHOP_MANAGER_INCENTIVE_EXPORT
  SHOP_MANAGER_INCENTIVE_SEND_TO_PAYROLL
  SHOP_MANAGER_INCENTIVE_INPUT_CONFIG_UPDATE
  SHOP_MANAGER_INCENTIVE_INPUT_CONFIG_ACTIVATE
  SHOP_MANAGER_INCENTIVE_INPUT_CONFIG_DEACTIVATE
  SHOP_MANAGER_INCENTIVE_READINESS_VALIDATE
  SHOP_MANAGER_INCENTIVE_RECALCULATION_REQUIRED
  SHOP_MANAGER_INCENTIVE_PAYROLL_TOTAL_CREATE
  SHOP_MANAGER_INCENTIVE_PAYROLL_TOTAL_UPDATE
  SHOP_MANAGER_INCENTIVE_PAYROLL_HANDOFF_BLOCKED
  PAYROLL_STATUTORY_PAYE_CREATE
  PAYROLL_STATUTORY_PAYE_UPDATE
  PAYROLL_STATUTORY_PAYE_APPROVE
  PAYROLL_STATUTORY_PAYE_DEACTIVATE
  PAYROLL_STATUTORY_PENSION_CREATE
  PAYROLL_STATUTORY_PENSION_UPDATE
  PAYROLL_STATUTORY_PENSION_APPROVE
  PAYROLL_STATUTORY_PENSION_DEACTIVATE
  PAYROLL_CALCULATION_READINESS_RUN
  PAYROLL_CALCULATION_PREVIEW
  PAYROLL_CALCULATION_START
  PAYROLL_CALCULATION_COMPLETE
  PAYROLL_CALCULATION_BLOCKED
  PAYROLL_CALCULATION_REVIEW_START
  PAYROLL_CALCULATION_APPROVE
  PAYROLL_CALCULATION_RETURN
  PAYROLL_CALCULATION_REOPEN
  PAYROLL_CALCULATION_EXPORT
  OTHER
}

model User {
  id                            String                            @id @default(uuid())
  email                         String                            @unique
  name                          String
  passwordHash                  String
  isActive                      Boolean                           @default(true)
  passwordChangeRequired        Boolean                           @default(false)
  failedLoginCount              Int                               @default(0)
  lockedUntil                   DateTime?
  lastLoginAt                   DateTime?
  employeeId                    String?
  createdAt                     DateTime                          @default(now())
  updatedAt                     DateTime                          @updatedAt
  roles                         UserRole[]
  auditLogs                     AuditLog[]
  notifications                 Notification[]
  imports                       ImportSession[]
  payComponentsCreated          PayComponent[]                    @relation("PayComponentCreatedBy")
  payComponentsUpdated          PayComponent[]                    @relation("PayComponentUpdatedBy")
  payRulesCreated               PayRule[]                         @relation("PayRuleCreatedBy")
  payRulesUpdated               PayRule[]                         @relation("PayRuleUpdatedBy")
  payrollPeriodsCreated         PayrollPeriod[]
  inputTypesCreated             PayrollInputType[]                @relation("InputTypeCreatedBy")
  inputTypesUpdated             PayrollInputType[]                @relation("InputTypeUpdatedBy")
  payrollPeriodEmployeesAdded   PayrollPeriodEmployee[]           @relation("PPEmployeeAddedBy")
  payrollPeriodEmployeesRemoved PayrollPeriodEmployee[]           @relation("PPEmployeeRemovedBy")
  payrollInputsSubmitted        PayrollInput[]                    @relation("PayrollInputSubmittedBy")
  payrollInputsLocked           PayrollInput[]                    @relation("PayrollInputLockedBy")
  inputRequirementsCreated      PayrollInputRequirement[]         @relation("InputRequirementCreatedBy")
  inputRequirementsUpdated      PayrollInputRequirement[]         @relation("InputRequirementUpdatedBy")
  inputWaiversCreated           PayrollInputWaiver[]              @relation("InputWaiverCreatedBy")
  shopProfilesCreated           ShopProfile[]                     @relation("ShopProfileCreator")
  shopProfilesUpdated           ShopProfile[]                     @relation("ShopProfileUpdater")
  shopCriteriaUpdated           ShopCriteriaStatusHistory[]       @relation("ShopCriteriaUpdater")
  shopCriteriaApproved          ShopCriteriaStatusHistory[]       @relation("ShopCriteriaApprover")
  incentivePeriodsCreated       ShopManagerIncentivePeriod[]      @relation("IncentivePeriodCreatedBy")
  incentiveInputsCreated        ShopManagerIncentiveInput[]       @relation("IncentiveInputCreatedBy")
  incentiveInputsUpdated        ShopManagerIncentiveInput[]       @relation("IncentiveInputUpdatedBy")
}

model Role {
  id          String           @id @default(uuid())
  name        String           @unique
  description String?
  users       UserRole[]
  permissions RolePermission[]
}

model Permission {
  id          String           @id @default(uuid())
  key         String           @unique
  description String?
  roles       RolePermission[]
}

model UserRole {
  userId String
  roleId String
  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)
  role   Role   @relation(fields: [roleId], references: [id], onDelete: Cascade)

  @@id([userId, roleId])
}

model RolePermission {
  roleId       String
  permissionId String
  role         Role       @relation(fields: [roleId], references: [id], onDelete: Cascade)
  permission   Permission @relation(fields: [permissionId], references: [id], onDelete: Cascade)

  @@id([roleId, permissionId])
}

model AuditLog {
  id         String      @id @default(uuid())
  userId     String?
  action     AuditAction
  entityType String
  entityId   String?
  oldValue   Json?
  newValue   Json?
  ipAddress  String?
  createdAt  DateTime    @default(now())
  user       User?       @relation(fields: [userId], references: [id])

  @@index([action, createdAt])
  @@index([entityType, entityId])
}

model Department {
  id        String       @id @default(uuid())
  name      String
  code      String       @unique(map: "dept_code_unique")
  parentId  String?
  headId    String?
  isActive  Boolean      @default(true)
  createdAt DateTime     @default(now())
  updatedAt DateTime     @updatedAt
  parent    Department?  @relation("DepartmentTree", fields: [parentId], references: [id])
  children  Department[] @relation("DepartmentTree")
  head      Employee?    @relation("DepartmentHead", fields: [headId], references: [id])

  @@index([parentId])
}

model Location {
  id                         String                            @id @default(uuid())
  name                       String
  code                       String                            @unique(map: "loc_code_unique")
  type                       LocationType
  parentId                   String?
  managerId                  String?
  isActive                   Boolean                           @default(true)
  createdAt                  DateTime                          @default(now())
  updatedAt                  DateTime                          @updatedAt
  parent                     Location?                         @relation("LocationTree", fields: [parentId], references: [id])
  children                   Location[]                        @relation("LocationTree")
  manager                    Employee?                         @relation("LocationManager", fields: [managerId], references: [id])
  shopProfile                ShopProfile?
  criteriaHistory            ShopCriteriaStatusHistory[]
  incentiveInputs      ShopManagerIncentiveInput[]
  incentiveCalculations ShopManagerIncentiveCalculation[]

  @@index([type])
  @@index([parentId])
}

model ShopProfile {
  id                   String       @id @default(uuid())
  shopLocationId       String       @unique(map: "shop_profile_location_unique")
  corridorType         CorridorType @default(UNKNOWN)
  defaultShopManagerId String?
  isIncentiveEligible  Boolean      @default(false)
  createdAt            DateTime     @default(now())
  updatedAt            DateTime     @updatedAt
  createdById          String?
  updatedById          String?
  shopLocation         Location     @relation(fields: [shopLocationId], references: [id])
  defaultShopManager   Employee?    @relation("ShopDefaultManager", fields: [defaultShopManagerId], references: [id])
  createdBy            User?        @relation("ShopProfileCreator", fields: [createdById], references: [id])
  updatedBy            User?        @relation("ShopProfileUpdater", fields: [updatedById], references: [id])
}

model ShopCriteriaStatusHistory {
  id             String       @id @default(uuid())
  shopLocationId String
  criteria       ShopCriteria @default(UNASSIGNED)
  effectiveFrom  DateTime
  effectiveTo    DateTime?
  reason         String?
  updatedById    String?
  approvedById   String?
  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
  shopLocation   Location     @relation(fields: [shopLocationId], references: [id])
  updatedBy      User?        @relation("ShopCriteriaUpdater", fields: [updatedById], references: [id])
  approvedBy     User?        @relation("ShopCriteriaApprover", fields: [approvedById], references: [id])

  @@index([shopLocationId, effectiveFrom])
  @@index([shopLocationId, effectiveTo])
}

model Employee {
  id                           String                            @id @default(uuid())
  employeeId                   String                            @unique(map: "emp_employee_id_unique")
  firstName                    String
  middleName                   String?
  lastName                     String
  fullName                     String
  gender                       String                            @default("NOT_SPECIFIED")
  dateOfBirth                  DateTime?
  phoneNumber                  String?
  email                        String?                           @unique(map: "emp_email_unique")
  address                      String?
  hireDate                     DateTime?
  employmentType               EmploymentType?
  employmentStatus             EmploymentStatus                  @default(DRAFT)
  employeeCategory             EmployeeCategory?
  probationStatus              String                            @default("NOT_APPLICABLE")
  currentDepartmentId          String?
  currentDivisionId            String?
  currentRegionId              String?
  currentAreaId                String?
  currentShopId                String?
  currentClusterId             String?
  currentRole                  EmployeeRole                      @default(OTHER)
  currentLevel                 EmployeeLevel                     @default(TO_BE_DEFINED)
  directManagerId              String?
  accountingReportingManagerId String?
  basicSalary                  Decimal?                          @db.Decimal(12, 2)
  salaryEffectiveDate          DateTime?
  notes                        String?
  createdById                  String?
  updatedById                  String?
  createdAt                    DateTime                          @default(now())
  updatedAt                    DateTime                          @updatedAt
  directManager                Employee?                         @relation("EmployeeManager", fields: [directManagerId], references: [id])
  accountingReportingManager   Employee?                         @relation("AccountingManager", fields: [accountingReportingManagerId], references: [id])
  accountingReportees          Employee[]                        @relation("AccountingManager")
  directReports                Employee[]                        @relation("EmployeeManager")
  departmentsLed               Department[]                      @relation("DepartmentHead")
  locationsManaged             Location[]                        @relation("LocationManager")
  assignments                  EmployeeAssignment[]
  statusHistory                EmployeeStatusHistory[]
  onboardingChecklist          OnboardingChecklist?
  documents                    EmployeeDocument[]
  leaveRecords                 LeaveRecord[]
  salaries                     EmployeeSalary[]
  salaryReviews                SalaryReview[]
  achievements                 Achievement[]
  disciplinaryRecords          DisciplinaryRecord[]
  terminations                 TerminationCase[]
  transferRequests             TransferRequest[]
  promotionRequests            PromotionRequest[]
  commissionCalculations       CommissionCalculation[]
  payrollRows                  PayrollPreparationRow[]
  payrollCalculationLines      PayrollCalculationLine[]
  payrollAttendance            PayrollAttendanceInput[]
  payrollAllowances            PayrollAllowance[]
  payrollDeductions            PayrollDeduction[]
  evaluations                  EmployeeEvaluation[]              @relation("EvaluationEmployee")
  evaluationsGiven             EmployeeEvaluation[]              @relation("EvaluationEvaluator")
  profileChangeRequests        EmployeeProfileChangeRequest[]
  payrollProfile               EmployeePayrollProfile?
  payrollPeriodEmployees       PayrollPeriodEmployee[]
  payrollInputs                PayrollInput[]
  payrollInputWaivers          PayrollInputWaiver[]
  shopDefaultManagerProfiles   ShopProfile[]                     @relation("ShopDefaultManager")
  incentiveInputs      ShopManagerIncentiveInput[]     @relation("IncentiveInputManager")
  incentiveCalculations ShopManagerIncentiveCalculation[] @relation("IncentiveCalculationManager")

  @@index([employmentStatus])
  @@index([currentRole])
  @@index([currentDepartmentId])
  @@index([directManagerId])
  @@index([accountingReportingManagerId])
  @@index([employeeCategory])
}

model EmployeeAssignment {
  id                           String            @id @default(uuid())
  employeeId                   String
  employeeCategory             EmployeeCategory?
  departmentId                 String?
  divisionId                   String?
  regionId                     String?
  areaId                       String?
  shopId                       String?
  clusterId                    String?
  role                         EmployeeRole
  level                        EmployeeLevel     @default(TO_BE_DEFINED)
  directManagerId              String?
  accountingReportingManagerId String?
  isActive                     Boolean           @default(true)
  startDate                    DateTime
  endDate                      DateTime?
  reason                       String?
  approvedById                 String?
  createdAt                    DateTime          @default(now())
  employee                     Employee          @relation(fields: [employeeId], references: [id], onDelete: Cascade)

  @@index([employeeId, endDate])
  @@index([employeeId, isActive])
}

model EmployeeStatusHistory {
  id             String            @id @default(uuid())
  employeeId     String
  previousStatus EmploymentStatus?
  newStatus      EmploymentStatus
  reason         String
  effectiveDate  DateTime
  updatedById    String?
  createdAt      DateTime          @default(now())
  employee       Employee          @relation(fields: [employeeId], references: [id], onDelete: Cascade)

  @@index([employeeId, effectiveDate])
}

model OnboardingChecklist {
  id          String                    @id @default(uuid())
  employeeId  String                    @unique
  status      ApprovalStatus            @default(DRAFT)
  completedAt DateTime?
  createdAt   DateTime                  @default(now())
  updatedAt   DateTime                  @updatedAt
  employee    Employee                  @relation(fields: [employeeId], references: [id], onDelete: Cascade)
  items       OnboardingChecklistItem[]
}

model OnboardingChecklistItem {
  id            String              @id @default(uuid())
  checklistId   String
  key           String
  label         String
  completed     Boolean             @default(false)
  completedById String?
  completedAt   DateTime?
  checklist     OnboardingChecklist @relation(fields: [checklistId], references: [id], onDelete: Cascade)

  @@unique([checklistId, key])
}

model EmployeeDocument {
  id                 String             @id @default(uuid())
  employeeId         String
  documentType       DocumentType
  filePath           String
  originalFilename   String?
  fileSize           Int?
  mimeType           String?
  uploadedById       String?
  uploadedAt         DateTime           @default(now())
  visibilityLevel    DocumentVisibility @default(PUBLIC_TO_HR)
  notes              String?
  isActive           Boolean            @default(true)
  deactivatedAt      DateTime?
  deactivatedById    String?
  deactivationReason String?
  updatedAt          DateTime           @updatedAt
  employee           Employee           @relation(fields: [employeeId], references: [id], onDelete: Cascade)

  @@index([employeeId, documentType])
  @@index([visibilityLevel, isActive])
}

model RequiredDocumentRule {
  id                         String            @id @default(uuid())
  name                       String
  documentType               DocumentType
  applicableEmploymentType   EmploymentType?
  applicableRole             EmployeeRole?
  applicableDepartmentId     String?
  applicableDivisionId       String?
  applicableEmployeeCategory EmployeeCategory?
  isRequired                 Boolean           @default(true)
  isActive                   Boolean           @default(true)
  createdAt                  DateTime          @default(now())
  updatedAt                  DateTime          @updatedAt
}

model LeaveRecord {
  id             String         @id @default(uuid())
  employeeId     String
  leaveType      LeaveType
  startDate      DateTime
  endDate        DateTime
  totalDays      Decimal?       @db.Decimal(6, 2)
  reason         String?
  approvalStatus ApprovalStatus @default(PENDING)
  requestedById  String?
  approvedById   String?
  approvedAt     DateTime?
  createdAt      DateTime       @default(now())
  updatedAt      DateTime       @updatedAt
  employee       Employee       @relation(fields: [employeeId], references: [id], onDelete: Cascade)

  @@index([employeeId, startDate])
}

model LeavePolicy {
  id                    String          @id @default(uuid())
  name                  String
  leaveType             LeaveType
  employmentType        EmploymentType?
  annualEntitlementDays Decimal         @db.Decimal(8, 2)
  carryForwardAllowed   Boolean         @default(false)
  maxCarryForwardDays   Decimal?        @db.Decimal(8, 2)
  isActive              Boolean         @default(true)
  effectiveStartDate    DateTime
  effectiveEndDate      DateTime?
  createdAt             DateTime        @default(now())
  updatedAt             DateTime        @updatedAt
}

model LeaveBalance {
  id             String    @id @default(uuid())
  employeeId     String
  leaveType      LeaveType
  periodStart    DateTime
  periodEnd      DateTime
  openingBalance Decimal   @default(0) @db.Decimal(8, 2)
  accruedDays    Decimal   @default(0) @db.Decimal(8, 2)
  usedDays       Decimal   @default(0) @db.Decimal(8, 2)
  adjustedDays   Decimal   @default(0) @db.Decimal(8, 2)
  closingBalance Decimal   @default(0) @db.Decimal(8, 2)
  updatedAt      DateTime  @updatedAt

  @@unique([employeeId, leaveType, periodStart, periodEnd])
}

model LeaveBalanceAdjustment {
  id             String         @id @default(uuid())
  employeeId     String
  leaveType      LeaveType
  adjustmentDays Decimal        @db.Decimal(8, 2)
  reason         String
  requestedById  String?
  approvedById   String?
  approvalStatus ApprovalStatus @default(SUBMITTED)
  createdAt      DateTime       @default(now())
  updatedAt      DateTime       @updatedAt
}

model EmployeeEvaluation {
  id                         String           @id @default(uuid())
  employeeId                 String
  evaluatorId                String
  evaluationPeriodStart      DateTime
  evaluationPeriodEnd        DateTime
  evaluationType             EvaluationType
  score                      Decimal?         @db.Decimal(5, 2)
  rating                     String?
  comments                   String?
  strengths                  String?
  improvementAreas           String?
  status                     EvaluationStatus @default(DRAFT)
  submittedDate              DateTime?
  attachmentPath             String?
  compensationRecommendation String           @default("NO_CHANGE")
  salaryReviewRequired       Boolean          @default(false)
  commissionReviewRequired   Boolean          @default(false)
  createdAt                  DateTime         @default(now())
  updatedAt                  DateTime         @updatedAt
  employee                   Employee         @relation("EvaluationEmployee", fields: [employeeId], references: [id], onDelete: Cascade)
  evaluator                  Employee         @relation("EvaluationEvaluator", fields: [evaluatorId], references: [id])

  @@index([employeeId, status])
  @@index([evaluatorId])
}

model Achievement {
  id              String         @id @default(uuid())
  employeeId      String
  title           String
  description     String?
  achievementDate DateTime
  createdById     String?
  approvedById    String?
  approvalStatus  ApprovalStatus @default(PENDING)
  createdAt       DateTime       @default(now())
  updatedAt       DateTime       @updatedAt
  employee        Employee       @relation(fields: [employeeId], references: [id], onDelete: Cascade)

  @@index([employeeId, approvalStatus])
}

model DisciplinaryRecord {
  id             String             @id @default(uuid())
  employeeId     String
  incidentType   String
  description    String
  actionTaken    String?
  warningLevel   String?
  incidentDate   DateTime
  issuedById     String?
  reviewedById   String?
  approvedById   String?
  attachmentPath String?
  followUpDate   DateTime?
  status         DisciplinaryStatus @default(DRAFT)
  createdAt      DateTime           @default(now())
  updatedAt      DateTime           @updatedAt
  employee       Employee           @relation(fields: [employeeId], references: [id], onDelete: Cascade)

  @@index([employeeId, status])
}

model TerminationCase {
  id                  String            @id @default(uuid())
  employeeId          String
  terminationType     String
  reason              String
  noticeDate          DateTime?
  lastWorkingDate     DateTime?
  initiatedById       String?
  reviewedById        String?
  approvedById        String?
  status              TerminationStatus @default(DRAFT)
  finalPaymentStatus  String            @default("NOT_STARTED")
  clearanceStatus     String            @default("NOT_STARTED")
  exitInterviewStatus String            @default("NOT_STARTED")
  notes               String?
  createdAt           DateTime          @default(now())
  updatedAt           DateTime          @updatedAt
  employee            Employee          @relation(fields: [employeeId], references: [id], onDelete: Cascade)

  @@index([employeeId, status])
}

model TransferRequest {
  id                    String         @id @default(uuid())
  employeeId            String
  currentAssignmentId   String?
  requestedDivisionId   String?
  requestedDepartmentId String?
  requestedRegionId     String?
  requestedShopId       String?
  requestedClusterId    String?
  requestedRole         EmployeeRole?
  requestedLevel        EmployeeLevel?
  reason                String
  effectiveDate         DateTime?
  initiatedById         String?
  reviewedById          String?
  approvedById          String?
  status                String         @default("DRAFT")
  createdAt             DateTime       @default(now())
  updatedAt             DateTime       @updatedAt
  employee              Employee       @relation(fields: [employeeId], references: [id], onDelete: Cascade)

  @@index([employeeId, status])
}

model PromotionRequest {
  id             String         @id @default(uuid())
  employeeId     String
  currentRole    EmployeeRole
  currentLevel   EmployeeLevel
  proposedRole   EmployeeRole?
  proposedLevel  EmployeeLevel?
  proposedSalary Decimal?       @db.Decimal(12, 2)
  reason         String
  effectiveDate  DateTime?
  initiatedById  String?
  reviewedById   String?
  approvedById   String?
  status         String         @default("DRAFT")
  createdAt      DateTime       @default(now())
  updatedAt      DateTime       @updatedAt
  employee       Employee       @relation(fields: [employeeId], references: [id], onDelete: Cascade)

  @@index([employeeId, status])
}

model ApprovalWorkflow {
  id           String            @id @default(uuid())
  name         String
  workflowType String
  isActive     Boolean           @default(true)
  createdById  String?
  createdAt    DateTime          @default(now())
  updatedAt    DateTime          @updatedAt
  steps        ApprovalStep[]
  requests     ApprovalRequest[]
}

model ApprovalStep {
  id                  String           @id @default(uuid())
  workflowId          String
  stepOrder           Int
  approverRole        String?
  approverUserId      String?
  isRequired          Boolean          @default(true)
  preventSelfApproval Boolean          @default(true)
  escalationAfterDays Int?
  createdAt           DateTime         @default(now())
  updatedAt           DateTime         @updatedAt
  workflow            ApprovalWorkflow @relation(fields: [workflowId], references: [id], onDelete: Cascade)
  actions             ApprovalAction[]

  @@unique([workflowId, stepOrder])
}

model ApprovalRequest {
  id            String           @id @default(uuid())
  workflowId    String
  entityType    String
  entityId      String
  requestedById String?
  currentStep   Int              @default(1)
  status        String           @default("SUBMITTED")
  createdAt     DateTime         @default(now())
  updatedAt     DateTime         @updatedAt
  workflow      ApprovalWorkflow @relation(fields: [workflowId], references: [id], onDelete: Cascade)
  actions       ApprovalAction[]

  @@index([entityType, entityId])
}

model ApprovalAction {
  id                String          @id @default(uuid())
  approvalRequestId String
  stepId            String?
  actionById        String?
  action            String
  comments          String?
  createdAt         DateTime        @default(now())
  approvalRequest   ApprovalRequest @relation(fields: [approvalRequestId], references: [id], onDelete: Cascade)
  step              ApprovalStep?   @relation(fields: [stepId], references: [id])
}

model Notification {
  id                String           @id @default(uuid())
  recipientUserId   String
  title             String
  message           String
  notificationType  NotificationType
  relatedEntityType String?
  relatedEntityId   String?
  isRead            Boolean          @default(false)
  readAt            DateTime?
  createdAt         DateTime         @default(now())
  recipientUser     User             @relation(fields: [recipientUserId], references: [id], onDelete: Cascade)

  @@index([recipientUserId, isRead])
}

model EmployeeSalary {
  id            String   @id @default(uuid())
  employeeId    String
  basicSalary   Decimal  @db.Decimal(12, 2)
  effectiveDate DateTime
  reason        String
  approvedById  String?
  createdById   String?
  createdAt     DateTime @default(now())
  employee      Employee @relation(fields: [employeeId], references: [id], onDelete: Cascade)

  @@index([employeeId, effectiveDate])
}

model SalaryReview {
  id                  String             @id @default(uuid())
  employeeId          String
  currentSalary       Decimal?           @db.Decimal(12, 2)
  proposedSalary      Decimal            @db.Decimal(12, 2)
  changeAmount        Decimal            @default(0) @db.Decimal(12, 2)
  changePercent       Decimal            @default(0) @db.Decimal(8, 2)
  reason              String
  relatedEvaluationId String?
  effectiveDate       DateTime?
  requestedById       String?
  reviewedById        String?
  approvedById        String?
  status              SalaryReviewStatus @default(DRAFT)
  hrReviewStatus      String             @default("PENDING")
  financeReviewStatus String             @default("NOT_REQUIRED")
  notes               String?
  createdAt           DateTime           @default(now())
  updatedAt           DateTime           @updatedAt
  employee            Employee           @relation(fields: [employeeId], references: [id], onDelete: Cascade)

  @@index([employeeId, status])
}

model CommissionPlan {
  id                 String                    @id @default(uuid())
  name               String
  description        String?
  applicableRole     EmployeeRole?
  calculationType    CommissionCalculationType
  rate               Decimal?                  @db.Decimal(10, 4)
  fixedAmount        Decimal?                  @db.Decimal(12, 2)
  capAmount          Decimal?                  @db.Decimal(12, 2)
  isActive           Boolean                   @default(true)
  effectiveStartDate DateTime
  effectiveEndDate   DateTime?
  createdById        String?
  createdAt          DateTime                  @default(now())
  updatedAt          DateTime                  @updatedAt
  calculations       CommissionCalculation[]
}

model CommissionCalculation {
  id                     String                      @id @default(uuid())
  employeeId             String
  commissionPlanId       String?
  periodStart            DateTime
  periodEnd              DateTime
  salesAmount            Decimal?                    @db.Decimal(12, 2)
  targetAmount           Decimal?                    @db.Decimal(12, 2)
  achievementPercent     Decimal?                    @db.Decimal(8, 2)
  calculatedCommission   Decimal                     @default(0) @db.Decimal(12, 2)
  manualAdjustment       Decimal                     @default(0) @db.Decimal(12, 2)
  manualAdjustmentReason String?
  finalCommission        Decimal                     @default(0) @db.Decimal(12, 2)
  status                 CommissionCalculationStatus @default(DRAFT)
  calculatedById         String?
  reviewedById           String?
  approvedById           String?
  notes                  String?
  createdAt              DateTime                    @default(now())
  updatedAt              DateTime                    @updatedAt
  employee               Employee                    @relation(fields: [employeeId], references: [id], onDelete: Cascade)
  commissionPlan         CommissionPlan?             @relation(fields: [commissionPlanId], references: [id])

  @@index([employeeId, periodStart, periodEnd])
}

model PayrollRule {
  id                 String    @id @default(uuid())
  ruleType           String
  name               String
  description        String?
  rate               Decimal?  @db.Decimal(10, 4)
  amount             Decimal?  @db.Decimal(12, 2)
  effectiveStartDate DateTime
  effectiveEndDate   DateTime?
  isActive           Boolean   @default(true)
  approvalStatus     String    @default("DRAFT")
  changeReason       String?
  isSample           Boolean   @default(false)
  createdById        String?
  createdAt          DateTime  @default(now())
  updatedAt          DateTime  @updatedAt

  @@index([ruleType, isActive])
}

model PayeTaxBracket {
  id                 String    @id @default(uuid())
  name               String
  minIncome          Decimal   @db.Decimal(12, 2)
  maxIncome          Decimal?  @db.Decimal(12, 2)
  taxRate            Decimal   @db.Decimal(10, 4)
  deductionAmount    Decimal   @default(0) @db.Decimal(12, 2)
  effectiveStartDate DateTime
  effectiveEndDate   DateTime?
  isActive           Boolean   @default(true)
  approvalStatus     String    @default("DRAFT")
  isSample           Boolean   @default(false)
  createdById        String?
  createdAt          DateTime  @default(now())
  updatedAt          DateTime  @updatedAt
}

model PensionRule {
  id                       String          @id @default(uuid())
  name                     String
  employeeRate             Decimal         @db.Decimal(10, 4)
  employerRate             Decimal         @db.Decimal(10, 4)
  pensionBaseType          PensionBaseType @default(BASIC_SALARY)
  minimumBase              Decimal?        @db.Decimal(12, 2)
  maximumBase              Decimal?        @db.Decimal(12, 2)
  priority                 Int             @default(0)
  applicableEmploymentType EmploymentType?
  applicableRole           EmployeeRole?
  effectiveStartDate       DateTime
  effectiveEndDate         DateTime?
  isActive                 Boolean         @default(true)
  approvalStatus           String          @default("DRAFT")
  isSample                 Boolean         @default(false)
  createdById              String?
  createdAt                DateTime        @default(now())
  updatedAt                DateTime        @updatedAt
}

model PayrollAttendanceInput {
  id                   String         @id @default(uuid())
  employeeId           String
  payrollPeriodStart   DateTime
  payrollPeriodEnd     DateTime
  workingDays          Decimal        @db.Decimal(8, 2)
  daysPresent          Decimal        @default(0) @db.Decimal(8, 2)
  daysAbsent           Decimal        @default(0) @db.Decimal(8, 2)
  paidLeaveDays        Decimal        @default(0) @db.Decimal(8, 2)
  unpaidLeaveDays      Decimal        @default(0) @db.Decimal(8, 2)
  sundayOvertimeHours  Decimal        @default(0) @db.Decimal(8, 2)
  holidayOvertimeHours Decimal        @default(0) @db.Decimal(8, 2)
  nightOvertimeHours   Decimal        @default(0) @db.Decimal(8, 2)
  approvalStatus       ApprovalStatus @default(DRAFT)
  approvedById         String?
  notes                String?
  createdAt            DateTime       @default(now())
  updatedAt            DateTime       @updatedAt
  employee             Employee       @relation(fields: [employeeId], references: [id], onDelete: Cascade)

  @@index([employeeId, payrollPeriodStart, payrollPeriodEnd])
}

model PayrollAllowance {
  id                 String         @id @default(uuid())
  employeeId         String
  payrollPeriodStart DateTime
  payrollPeriodEnd   DateTime
  allowanceType      AllowanceType
  amount             Decimal        @db.Decimal(12, 2)
  isTaxable          Boolean        @default(true)
  approvalStatus     ApprovalStatus @default(DRAFT)
  reason             String?
  createdById        String?
  approvedById       String?
  createdAt          DateTime       @default(now())
  updatedAt          DateTime       @updatedAt
  employee           Employee       @relation(fields: [employeeId], references: [id], onDelete: Cascade)

  @@index([employeeId, payrollPeriodStart, payrollPeriodEnd])
}

model PayrollDeduction {
  id                 String         @id @default(uuid())
  employeeId         String
  payrollPeriodStart DateTime
  payrollPeriodEnd   DateTime
  deductionType      DeductionType
  amount             Decimal        @db.Decimal(12, 2)
  isPreTax           Boolean        @default(false)
  approvalStatus     ApprovalStatus @default(DRAFT)
  reason             String?
  createdById        String?
  approvedById       String?
  createdAt          DateTime       @default(now())
  updatedAt          DateTime       @updatedAt
  employee           Employee       @relation(fields: [employeeId], references: [id], onDelete: Cascade)

  @@index([employeeId, payrollPeriodStart, payrollPeriodEnd])
}

model PayrollPreparationBatch {
  id                    String                    @id @default(uuid())
  payrollPeriodId       String
  version               Int                       @default(1)
  batchName             String
  payrollPeriodStart    DateTime
  payrollPeriodEnd      DateTime
  calculationStatus     PayrollCalculationStatus? @default(PENDING)
  status                PayrollBatchStatus        @default(DRAFT)
  calculationStartedAt  DateTime?
  calculationCompletedAt DateTime?
  calculatedById        String?
  reviewedById          String?
  reviewedAt            DateTime?
  approvedById          String?
  approvedAt            DateTime?
  lockedById            String?
  lockedAt              DateTime?
  calculationHash       String?
  employeeCount         Int                       @default(0)
  blockerCount          Int                       @default(0)
  warningCount          Int                       @default(0)
  grossEarningsTotal    Decimal?                  @db.Decimal(14, 2)
  taxableIncomeTotal    Decimal?                  @db.Decimal(14, 2)
  employeePensionTotal  Decimal?                  @db.Decimal(14, 2)
  employerPensionTotal  Decimal?                  @db.Decimal(14, 2)
  payeTaxTotal          Decimal?                  @db.Decimal(14, 2)
  otherDeductionTotal   Decimal?                  @db.Decimal(14, 2)
  netSalaryTotal        Decimal?                  @db.Decimal(14, 2)
  employerTotalCost     Decimal?                  @db.Decimal(14, 2)
  notes                 String?
  createdById           String?
  createdAt             DateTime                  @default(now())
  updatedAt             DateTime                  @updatedAt
  payrollPeriod         PayrollPeriod             @relation(fields: [payrollPeriodId], references: [id])
  rows                  PayrollPreparationRow[]
  calculationLines      PayrollCalculationLine[]

  @@unique([payrollPeriodId, version])
  @@index([payrollPeriodId])
  @@index([status])
}

model PayrollPreparationRow {
  id                        String                  @id @default(uuid())
  batchId                   String
  payrollPeriodId           String
  employeeId                String
  employeeCode              String
  fullName                  String
  employmentType            String?
  division                  String?
  department                String?
  region                    String?
  shop                      String?
  role                      String
  level                     String
  basicSalary               Decimal?                @db.Decimal(12, 2)
  salarySource              String?
  salaryEffectiveDate       DateTime?
  proratedBasicSalary       Decimal?                @db.Decimal(12, 2)
  workingDays               Decimal?                @db.Decimal(8, 2)
  daysPresent               Decimal?                @db.Decimal(8, 2)
  paidLeaveDays             Decimal?                @db.Decimal(8, 2)
  unpaidLeaveDays           Decimal?                @db.Decimal(8, 2)
  grossTaxableEarnings      Decimal?                @db.Decimal(12, 2)
  grossNonTaxableEarnings   Decimal?                @db.Decimal(12, 2)
  pensionableIncome         Decimal?                @db.Decimal(12, 2)
  approvedAllowances        Decimal?                @db.Decimal(12, 2)
  approvedCommission        Decimal?                @db.Decimal(12, 2)
  overtimeAmount            Decimal?                @db.Decimal(12, 2)
  preTaxDeductions          Decimal?                @db.Decimal(12, 2)
  postTaxDeductions         Decimal?                @db.Decimal(12, 2)
  totalDeductions           Decimal?                @db.Decimal(12, 2)
  grossSalary               Decimal?                @db.Decimal(12, 2)
  taxableIncome             Decimal?                @db.Decimal(12, 2)
  employeePension           Decimal?                @db.Decimal(12, 2)
  employerPension           Decimal?                @db.Decimal(12, 2)
  payeTax                   Decimal?                @db.Decimal(12, 2)
  netSalary                 Decimal?                @db.Decimal(12, 2)
  employerTotalCost         Decimal?                @db.Decimal(12, 2)
  otherEmployerContributions Decimal?               @db.Decimal(12, 2)
  manualAdjustments         Decimal?                @db.Decimal(12, 2)
  approvedDeductions        Decimal?                @db.Decimal(12, 2)
  readinessStatus           PayrollReadiness
  calculationVersion        Int                     @default(1)
  calculatedAt              DateTime?
  calculatedById            String?
  blockers                  Json?
  warnings                  Json?
  calculationBreakdown      Json?
  includedInExport          Boolean                 @default(true)
  createdAt                 DateTime                @default(now())
  updatedAt                 DateTime                @updatedAt
  batch                     PayrollPreparationBatch @relation(fields: [batchId], references: [id], onDelete: Cascade)
  employee                  Employee                @relation(fields: [employeeId], references: [id], onDelete: Cascade)
  calculationLines          PayrollCalculationLine[]

  @@unique([batchId, employeeId])
  @@index([payrollPeriodId])
}

model PayrollCalculationLine {
  id               String               @id @default(uuid())
  batchId          String
  rowId            String?
  payrollPeriodId  String
  employeeId       String
  componentId      String?
  componentCode    String
  componentName    String
  lineType         CalculationLineType
  sourceType       CalculationSourceType
  sourceId         String?
  quantity         Decimal?             @db.Decimal(12, 4)
  rate             Decimal?             @db.Decimal(12, 4)
  baseAmount       Decimal?             @db.Decimal(12, 2)
  grossAmount      Decimal?             @default(0) @db.Decimal(12, 2)
  taxableAmount    Decimal?             @default(0) @db.Decimal(12, 2)
  nonTaxableAmount Decimal?             @default(0) @db.Decimal(12, 2)
  pensionableAmount Decimal?            @default(0) @db.Decimal(12, 2)
  deductionAmount  Decimal?             @default(0) @db.Decimal(12, 2)
  employerAmount   Decimal?             @default(0) @db.Decimal(12, 2)
  ruleId           String?
  calculationOrder Int                  @default(0)
  calculationNote  String?
  createdAt        DateTime             @default(now())
  batch            PayrollPreparationBatch @relation(fields: [batchId], references: [id], onDelete: Cascade)
  row              PayrollPreparationRow?  @relation(fields: [rowId], references: [id], onDelete: SetNull)
  employee         Employee             @relation(fields: [employeeId], references: [id], onDelete: Cascade)

  @@index([batchId])
  @@index([rowId])
  @@index([employeeId])
  @@index([payrollPeriodId])
}

model PayrollPeriodLock {
  id                  String            @id @default(uuid())
  payrollPeriodStart  DateTime
  payrollPeriodEnd    DateTime
  lockStatus          PayrollLockStatus @default(UNLOCKED)
  lockedById          String?
  lockedAt            DateTime?
  unlockRequestedById String?
  unlockedById        String?
  unlockedAt          DateTime?
  reason              String?
  createdAt           DateTime          @default(now())
  updatedAt           DateTime          @updatedAt

  @@unique([payrollPeriodStart, payrollPeriodEnd])
}

model PayrollAdjustment {
  id                 String         @id @default(uuid())
  employeeId         String
  payrollPeriodStart DateTime
  payrollPeriodEnd   DateTime
  adjustmentType     String
  amount             Decimal        @db.Decimal(12, 2)
  reason             String
  requestedById      String?
  approvedById       String?
  approvalStatus     ApprovalStatus @default(SUBMITTED)
  createdAt          DateTime       @default(now())
  updatedAt          DateTime       @updatedAt

  @@index([employeeId, payrollPeriodStart, payrollPeriodEnd])
}

model KpiMetric {
  id          String   @id @default(uuid())
  name        String   @unique
  description String?
  unit        String?
  isActive    Boolean  @default(true)
  createdById String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model AttendanceRecord {
  id             String         @id @default(uuid())
  employeeId     String
  attendanceDate DateTime
  status         String
  checkInTime    DateTime?
  checkOutTime   DateTime?
  hoursWorked    Decimal?       @db.Decimal(8, 2)
  overtimeHours  Decimal?       @db.Decimal(8, 2)
  approvalStatus ApprovalStatus @default(SUBMITTED)
  recordedById   String?
  approvedById   String?
  notes          String?
  createdAt      DateTime       @default(now())
  updatedAt      DateTime       @updatedAt

  @@unique([employeeId, attendanceDate])
}

model DataQualityIssue {
  id           String    @id @default(uuid())
  issueType    String
  severity     String
  employeeId   String?
  description  String
  suggestedFix String?
  status       String    @default("OPEN")
  assignedToId String?
  resolvedById String?
  resolvedAt   DateTime?
  ignoredById  String?
  ignoredAt    DateTime?
  ignoreReason String?
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt

  @@index([severity, status])
}

model EmployeeProfileChangeRequest {
  id             String    @id @default(uuid())
  employeeId     String
  requestedField String
  oldValue       String?
  newValue       String
  reason         String?
  status         String    @default("SUBMITTED")
  requestedById  String?
  reviewedById   String?
  approvedById   String?
  rejectedById   String?
  reviewComment  String?
  approvedAt     DateTime?
  rejectedAt     DateTime?
  appliedAt      DateTime?
  createdAt      DateTime  @default(now())
  updatedAt      DateTime  @updatedAt
  employee       Employee  @relation(fields: [employeeId], references: [id], onDelete: Cascade)

  @@index([employeeId, status])
}

model SalaryRuleApprovalRequest {
  id             String    @id @default(uuid())
  ruleId         String
  actionType     String
  status         String    @default("SUBMITTED")
  requestedById  String?
  reviewedById   String?
  approvedById   String?
  rejectedById   String?
  requestReason  String?
  reviewComment  String?
  validationJson String?
  createdAt      DateTime  @default(now())
  updatedAt      DateTime  @updatedAt
  approvedAt     DateTime?
  rejectedAt     DateTime?
  appliedAt      DateTime?
  rule           PayRule   @relation(fields: [ruleId], references: [id], onDelete: Cascade)

  @@index([ruleId, status])
}

model PhaseControlChecklist {
  id          String   @id @default(uuid())
  section     String
  item        String
  status      String   @default("NOT_STARTED")
  owner       String?
  comment     String?
  updatedById String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@unique([section, item])
}

model SystemSetting {
  id          String   @id @default(uuid())
  key         String   @unique
  value       String
  description String?
  isSensitive Boolean  @default(false)
  updatedById String?
  updatedAt   DateTime @updatedAt
}

model ExportHistory {
  id          String   @id @default(uuid())
  exportType  String
  format      String
  rowCount    Int
  fileName    String?
  createdById String?
  metadata    Json?
  createdAt   DateTime @default(now())
}

model EmployeePayrollProfile {
  id                String   @id @default(uuid())
  employeeId        String   @unique
  paymentMethod     String?
  bankName          String?
  bankAccountNumber String?
  mpesaAccount      String?
  taxId             String?
  pensionId         String?
  costCenter        String?
  updatedById       String?
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt
  employee          Employee @relation(fields: [employeeId], references: [id], onDelete: Cascade)
}

model PayComponent {
  id                String           @id @default(uuid())
  code              String           @unique
  name              String
  description       String?
  componentType     PayComponentType @default(OTHER)
  taxTreatment      TaxTreatment     @default(UNKNOWN)
  isEarning         Boolean          @default(true)
  isDeduction       Boolean          @default(false)
  isStatutory       Boolean          @default(false)
  isVariable        Boolean          @default(false)
  isActive          Boolean          @default(true)
  isPensionable     Boolean          @default(false)
  taxablePercent    Decimal          @default(0) @db.Decimal(5, 2)
  pensionablePercent Decimal         @default(0) @db.Decimal(5, 2)
  affectsGross      Boolean          @default(true)
  affectsNet        Boolean          @default(true)
  affectsEmployerCost Boolean         @default(false)
  calculationOrder  Int              @default(0)
  createdById       String?
  updatedById       String?
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt
  createdBy         User?            @relation("PayComponentCreatedBy", fields: [createdById], references: [id])
  updatedBy         User?            @relation("PayComponentUpdatedBy", fields: [updatedById], references: [id])
  rules             PayRule[]        @relation("PayRuleComponent")
  inputTypes        PayrollInputType[]
}

model PayRule {
  id                  String                      @id @default(uuid())
  componentId         String
  name                String
  description         String?
  employeeCategory    EmployeeCategory?
  role                EmployeeRole?
  departmentId        String?
  regionId            String?
  areaId              String?
  shopId              String?
  employmentType      EmploymentType?
  ruleType            PayRuleType                 @default(FIXED_AMOUNT)
  calculationMethod   PayRuleType                 @default(FIXED_AMOUNT)
  baseAmount          Decimal?                    @db.Decimal(12, 2)
  percentageRate      Decimal?                    @db.Decimal(8, 4)
  maxAmount           Decimal?                    @db.Decimal(12, 2)
  minAmount           Decimal?                    @db.Decimal(12, 2)
  thresholdValue      Decimal?                    @db.Decimal(12, 4)
  thresholdMetric     ThresholdMetric?
  tierConfigJson      String?
  formulaJson         String?
  requiresManualInput Boolean                     @default(false)
  requiresApproval    Boolean                     @default(false)
  effectiveFrom       DateTime
  effectiveTo         DateTime?
  status              PayRuleStatus               @default(DRAFT)
  priority            Int                         @default(0)
  createdById         String?
  updatedById         String?
  createdAt           DateTime                    @default(now())
  updatedAt           DateTime                    @updatedAt
  component           PayComponent                @relation("PayRuleComponent", fields: [componentId], references: [id])
  createdBy           User?                       @relation("PayRuleCreatedBy", fields: [createdById], references: [id])
  updatedBy           User?                       @relation("PayRuleUpdatedBy", fields: [updatedById], references: [id])
  approvalRequests    SalaryRuleApprovalRequest[]

  @@index([componentId, status])
  @@index([role, employeeCategory, status])
  @@index([effectiveFrom, effectiveTo])
}

enum ImportStatus {
  PENDING
  PROCESSING
  COMPLETED
  FAILED
  CANCELLED
}

enum ImportMode {
  CREATE_ONLY
  UPDATE_ONLY
  CREATE_OR_UPDATE
}

enum ImportRowStatus {
  PENDING
  VALID
  WARNING
  ERROR
  DUPLICATE
  SKIPPED
}

model ImportSession {
  id            String       @id @default(uuid())
  fileName      String
  importMode    ImportMode
  status        ImportStatus @default(PENDING)
  totalRows     Int          @default(0)
  validRows     Int          @default(0)
  warningRows   Int          @default(0)
  errorRows     Int          @default(0)
  duplicateRows Int          @default(0)
  createdCount  Int          @default(0)
  updatedCount  Int          @default(0)
  skippedCount  Int          @default(0)
  uploadedById  String?
  completedAt   DateTime?
  errorMessage  String?
  metadata      Json?
  createdAt     DateTime     @default(now())
  rows          ImportRow[]
  uploadedBy    User?        @relation(fields: [uploadedById], references: [id])
}

model ImportRow {
  id                String          @id @default(uuid())
  sessionId         String
  rowNumber         Int
  status            ImportRowStatus @default(PENDING)
  errors            String          @default("[]")
  warnings          String          @default("[]")
  matchedEmployeeId String?
  rawRow            Json
  parsedData        Json?
  createdAt         DateTime        @default(now())
  session           ImportSession   @relation(fields: [sessionId], references: [id], onDelete: Cascade)

  @@index([sessionId, status])
}

model PayrollPeriod {
  id              String                      @id @default(uuid())
  periodName      String
  periodStart     DateTime
  periodEnd       DateTime
  payDate         DateTime
  status          PayrollPeriodStatus         @default(DRAFT)
  createdById     String?
  createdAt       DateTime                    @default(now())
  updatedAt       DateTime                    @updatedAt
  createdBy       User?                       @relation(fields: [createdById], references: [id])
  employees       PayrollPeriodEmployee[]
  inputs          PayrollInput[]
  waivers         PayrollInputWaiver[]
  incentivePeriod ShopManagerIncentivePeriod?
  batches         PayrollPreparationBatch[]

  @@index([status])
  @@index([createdById])
}

model PayrollInputType {
  id               String                    @id @default(uuid())
  code             String                    @unique
  name             String
  description      String?
  category         PayrollInputTypeCategory
  valueType        PayrollInputTypeValueType
  defaultAmount    Decimal?                  @db.Decimal(12, 2)
  requiresApproval Boolean                   @default(false)
  isActive         Boolean                   @default(true)
  payComponentId   String?
  createdById      String?
  updatedById      String?
  createdAt        DateTime                  @default(now())
  updatedAt        DateTime                  @updatedAt
  createdBy        User?                     @relation("InputTypeCreatedBy", fields: [createdById], references: [id])
  updatedBy        User?                     @relation("InputTypeUpdatedBy", fields: [updatedById], references: [id])
  payComponent     PayComponent?             @relation(fields: [payComponentId], references: [id])
  inputs           PayrollInput[]
  requirements     PayrollInputRequirement[]
  waivers          PayrollInputWaiver[]

  @@index([code])
  @@index([category])
  @@index([isActive])
  @@index([payComponentId])
}

model PayrollPeriodEmployee {
  id              String        @id @default(uuid())
  payrollPeriodId String
  employeeId      String
  addedById       String?
  addedAt         DateTime      @default(now())
  removedAt       DateTime?
  removedById     String?
  isSelected      Boolean       @default(true)
  payrollPeriod   PayrollPeriod @relation(fields: [payrollPeriodId], references: [id], onDelete: Cascade)
  employee        Employee      @relation(fields: [employeeId], references: [id], onDelete: Cascade)
  addedBy         User?         @relation("PPEmployeeAddedBy", fields: [addedById], references: [id])
  removedBy       User?         @relation("PPEmployeeRemovedBy", fields: [removedById], references: [id])

  @@unique([payrollPeriodId, employeeId])
  @@index([payrollPeriodId, isSelected])
  @@index([employeeId])
}

model PayrollInput {
  id              String             @id @default(uuid())
  payrollPeriodId String
  employeeId      String
  inputTypeId     String
  value           Decimal?           @db.Decimal(12, 2)
  amount          Decimal?           @db.Decimal(12, 2)
  note            String?
  source          PayrollInputSource @default(MANUAL)
  status          PayrollInputStatus @default(DRAFT)
  submittedById   String?
  submittedAt     DateTime?
  isLocked        Boolean            @default(false)
  lockedById      String?
  lockedAt        DateTime?
  lockReason      String?
  createdAt       DateTime           @default(now())
  updatedAt       DateTime           @updatedAt
  payrollPeriod   PayrollPeriod      @relation(fields: [payrollPeriodId], references: [id], onDelete: Cascade)
  employee        Employee           @relation(fields: [employeeId], references: [id], onDelete: Cascade)
  inputType       PayrollInputType   @relation(fields: [inputTypeId], references: [id])
  submittedBy     User?              @relation("PayrollInputSubmittedBy", fields: [submittedById], references: [id])
  lockedBy        User?              @relation("PayrollInputLockedBy", fields: [lockedById], references: [id])

  @@unique([payrollPeriodId, employeeId, inputTypeId])
  @@index([payrollPeriodId, employeeId])
  @@index([payrollPeriodId, inputTypeId])
  @@index([payrollPeriodId, status])
  @@index([employeeId])
}

model PayrollInputRequirement {
  id               String            @id @default(uuid())
  inputTypeId      String
  employeeCategory EmployeeCategory?
  role             EmployeeRole?
  departmentId     String?
  regionId         String?
  areaId           String?
  shopId           String?
  employmentType   EmploymentType?
  isRequired       Boolean           @default(true)
  severity         String            @default("BLOCKER")
  isActive         Boolean           @default(true)
  createdById      String?
  updatedById      String?
  createdAt        DateTime          @default(now())
  updatedAt        DateTime          @updatedAt
  inputType        PayrollInputType  @relation(fields: [inputTypeId], references: [id])
  createdBy        User?             @relation("InputRequirementCreatedBy", fields: [createdById], references: [id])
  updatedBy        User?             @relation("InputRequirementUpdatedBy", fields: [updatedById], references: [id])

  @@index([inputTypeId, isActive])
  @@index([employeeCategory, role])
}

model PayrollInputWaiver {
  id              String           @id @default(uuid())
  payrollPeriodId String
  employeeId      String
  inputTypeId     String
  reason          String
  severity        String           @default("WARNING")
  createdById     String?
  createdAt       DateTime         @default(now())
  isActive        Boolean          @default(true)
  payrollPeriod   PayrollPeriod    @relation(fields: [payrollPeriodId], references: [id], onDelete: Cascade)
  employee        Employee         @relation(fields: [employeeId], references: [id], onDelete: Cascade)
  inputType       PayrollInputType @relation(fields: [inputTypeId], references: [id])
  createdBy       User?            @relation("InputWaiverCreatedBy", fields: [createdById], references: [id])

  @@unique([payrollPeriodId, employeeId, inputTypeId])
  @@index([payrollPeriodId])
  @@index([employeeId])
}

model ShopManagerIncentivePeriod {
  id              String                          @id @default(uuid())
  payrollPeriodId String                          @unique
  name            String
  month           Int
  year            Int
  status          IncentivePeriodStatus           @default(DRAFT)
  createdById     String?
  createdAt       DateTime                        @default(now())
  updatedAt       DateTime                        @updatedAt
  payrollPeriod   PayrollPeriod                   @relation(fields: [payrollPeriodId], references: [id])
  createdBy       User?                           @relation("IncentivePeriodCreatedBy", fields: [createdById], references: [id])
  inputs          ShopManagerIncentiveInput[]
  calculations    ShopManagerIncentiveCalculation[]

  @@index([payrollPeriodId])
  @@index([status])
  @@index([month, year])
}

model ShopManagerIncentiveInput {
  id                           String       @id @default(uuid())
  incentivePeriodId            String
  shopLocationId               String
  shopManagerId                String?
  shopCriteria                 ShopCriteria?
  corridorStatus               Boolean?
  qgaAbove90                   Boolean?
  qgaQuantity                  Int?
  mmQoAbove90                  Boolean?
  dsaAirtimeAchievementPercent Decimal?     @db.Decimal(6, 2)
  evdAbove100AndReconciled     Boolean?
  mpesaTargetAndReconciled     Boolean?
  mpesaFloatSold               Decimal?     @db.Decimal(12, 2)
  baSite                       Boolean?
  ebuTargetAchieved            Boolean?
  ebuRevenueMade               Boolean?
  ebuAverageTopupAbove500      Boolean?
  ebuFirstMonthLfRevenue       Decimal?     @db.Decimal(12, 2)
  responsibleRemarks           String?
  createdById                  String?
  updatedById                  String?
  createdAt                    DateTime     @default(now())
  updatedAt                    DateTime     @updatedAt
  incentivePeriod              ShopManagerIncentivePeriod @relation(fields: [incentivePeriodId], references: [id], onDelete: Cascade)
  shopLocation                 Location     @relation(fields: [shopLocationId], references: [id])
  shopManager                  Employee?    @relation("IncentiveInputManager", fields: [shopManagerId], references: [id])
  createdBy                    User?        @relation("IncentiveInputCreatedBy", fields: [createdById], references: [id])
  updatedBy                    User?        @relation("IncentiveInputUpdatedBy", fields: [updatedById], references: [id])
  calculation                  ShopManagerIncentiveCalculation?

  @@unique([incentivePeriodId, shopLocationId])
  @@index([incentivePeriodId])
  @@index([shopLocationId])
}

model ShopManagerIncentiveCalculation {
  id                 String    @id @default(uuid())
  incentivePeriodId  String
  inputId            String?   @unique
  shopLocationId     String
  shopManagerId      String?
  shopCriteria       ShopCriteria?
  qgaBonus           Decimal?  @db.Decimal(12, 2)
  qgaSimCommission   Decimal?  @db.Decimal(12, 2)
  evdBonus           Decimal?  @db.Decimal(12, 2)
  mpesaCommission    Decimal?  @db.Decimal(12, 2)
  baSiteBonus        Decimal?  @db.Decimal(12, 2)
  dsaAchievementBonus Decimal? @db.Decimal(12, 2)
  qoBonus            Decimal?  @db.Decimal(12, 2)
  ebuActivationBonus Decimal?  @db.Decimal(12, 2)
  ebuRevenueShare    Decimal?  @db.Decimal(12, 2)
  totalIncentive     Decimal?  @db.Decimal(12, 2)
  calculationNote    String?
  calculatedAt       DateTime?
  createdAt          DateTime  @default(now())
  updatedAt          DateTime  @updatedAt
  incentivePeriod    ShopManagerIncentivePeriod @relation(fields: [incentivePeriodId], references: [id], onDelete: Cascade)
  input              ShopManagerIncentiveInput? @relation(fields: [inputId], references: [id])
  shopLocation       Location  @relation(fields: [shopLocationId], references: [id])
  shopManager        Employee? @relation("IncentiveCalculationManager", fields: [shopManagerId], references: [id])

  @@unique([incentivePeriodId, shopLocationId])
  @@index([incentivePeriodId])
  @@index([shopLocationId])
}

model ShopManagerIncentiveInputConfig {
  id                     String  @id @default(uuid())
  inputCode              String  @unique
  inputLabel             String
  ownerDepartment        String
  ownerRole              String
  inputType              String
  allowedValues          String?
  usedInComponent        String?
  displayOrder           Int     @default(0)
  isActive               Boolean @default(true)
  isRequired             Boolean @default(false)
  blocksCalculation      Boolean @default(false)
  blocksPayrollHandoff   Boolean @default(false)
  minValue               Decimal? @db.Decimal(12, 2)
  maxValue               Decimal? @db.Decimal(12, 2)
  helpText               String?
  requiredWhenJson       String?
}
